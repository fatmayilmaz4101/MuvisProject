import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        color: 'white',
        borderWidth: 1,
        borderColor: '#169D6B',
        borderRadius: 15,
        marginTop: 10,
        padding: 10,
        width: '80%',    
    }
  });