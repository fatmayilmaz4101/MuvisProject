import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  updateButton: {
    width: 200,
    margin: 10,
    padding: 12,
    borderRadius: 30,
  },
  updateButtonText: {
    fontSize: 16,
  },
});
