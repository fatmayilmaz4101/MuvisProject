import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  personIcon: {
    fontSize: 20,
    marginLeft: 20,
  },
  headerStyle: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});
