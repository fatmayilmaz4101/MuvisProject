import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  textStyle: {
    alignItems: 'center',
  },
  greetingText: {
    fontSize: 20,
    fontWeight: 'bold',
    color: '#169D6B',
    marginTop: 20,
  },
});
