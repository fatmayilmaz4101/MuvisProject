export interface MovieType {
    id: number;
    title: string;
    thumbnailUrl: string;
  }
  export interface UserType {
    id: string,
    firstName: string,
    lastName: string,
    userName: string,
    password: string,
    phone: number
}
